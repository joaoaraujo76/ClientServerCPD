package protocol;

public enum MessageType {
    REGISTER,
    REGISTERED,
    LOGIN,
    LOGIN_TOKEN,
    AUTHENTICATED,
    AUTHENTICATED_TOKEN,
    JOIN_QUEUE,
    LEAVE_QUEUE,
    QUEUE_POSITION,
    QUEUE_LEFT,
    RESUME,
    ERROR
}


